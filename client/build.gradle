dependencies {
    implementation "$corda_release_group:corda-client-extensions-rpc:$default_library_version"
    implementation "$corda_release_group:corda-client-rpc:$default_library_version"
    implementation "$corda_release_group:corda-flows:$default_library_version"
    implementation "$corda_release_group:corda-ledger:$default_library_version"
    implementation "$corda_release_group:corda-legacy-api:$default_library_version"

    implementation project(":cordapp")
    implementation project(":cordapp2")
}

task run(type: JavaExec) {
    classpath = sourceSets.main.runtimeClasspath
    main = "net.corda.joel.client.TestClientKt"
}