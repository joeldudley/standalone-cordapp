dependencies {
    implementation "$cordaReleaseGroup:corda-client-extensions-rpc:$defaultLibraryVersion"
    implementation "$cordaReleaseGroup:corda-client-rpc:$defaultLibraryVersion"
    implementation "$cordaReleaseGroup:corda-ledger:$defaultLibraryVersion"
    implementation "$cordaReleaseGroup:corda-legacy-api:$defaultLibraryVersion"

    implementation project(":cordapps:cordapp-one")
    implementation project(":cordapps:cordapp-two")
}

task run(type: JavaExec) {
    classpath = sourceSets.main.runtimeClasspath
    main = "net.corda.joel.client.TestClientKt"
}